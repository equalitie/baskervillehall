apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgres-baskervillehall-dev-pv
  labels:
    type: local
spec:
  storageClassName: csi-expandable
  capacity:
    storage: 30Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/data"