apiVersion: v1
kind: Service
metadata:
  name: postgres-baskervillehall-dev
  labels:
    app: postgres-baskervillehall-dev
spec:
  selector:
    app: postgres-baskervillehall-dev
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432